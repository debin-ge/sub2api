# 通用视频任务平台收口清单

> 冻结日期：2026-09-05（America/Los_Angeles；对应 UTC 2026-09-06）。
> 用户决定：停止继续扩展修复范围，整理已有成果、明确必要性并进行有边界的验收。
> 最新范围纠正：用户明确原需求不包含 Grok 兼容与处理。Grok 从本次交付、待决和验收范围排除，不再要求存量盘点、兼容、迁移或更正；此前实现过这些内容不构成需求授权。
> 最新环境确认：生产没有执行本次视频平台迁移和处理，本地开发环境允许直接调整；不再为未发布的 Grok SQL 增加历史兼容、退役迁移或归档层。
> 当前结论：第 1-24 批功能扩展冻结；新增 Grok 运行时、未发布 SQL 与残留代码已按用户授权清理，后续审查的四项问题已修复，最新验证见《通用视频任务平台Grok剥离记录》第 8 节。下方前轮冻结证据不代表最新修复后的结果，未获生产发布通过。
> 本文是当前范围、待办和验收状态的唯一汇总；其他九份文档的批次说明是历史证据，不构成继续开发指令。
> 首发范围再次更新（2026-09-06）：已开放编辑、延长、角色和可选回调，并补齐 Seedance 2.0 参数与 `content[]` 兼容；验收标准与授权边界见《原生视频首发实施与验收方案》。以下“运行时关闭”结论仅为此前历史状态。
> 本地实施更新：当前运行手册、smoke/对账工具、进程退出/隔离升级验证、页面防重复操作和删除积压告警已落地；最新验证与外部缺口见首发方案第 6 节，不能据此宣布生产验收通过。

## 1. 本轮范围与停止条件

当前收尾包含已授权的新增 Grok 运行时、未发布 SQL 与死代码清理、四项审查修复，以及编辑/延长/角色/回调扩展和本地回归。保留 OpenAI 视频链路所需的安全校验、幂等及恢复能力，不把“代码已经存在”当作保留新增 Grok 实现的理由，不触碰项目原有 Grok 功能。

- 允许修复：验收发现且有反例的越权、重复提交/扣费、资金丢失、恢复中断或构建阻塞；先记录复现、影响和最小改动范围，再修复与复验。
- 不继续扩展：新 Provider、全系统 reservation、自动身份验真、可信媒体探测、历史更正新能力，以及不存在实际存量证据的迁移需求。
- 不执行：生产迁移/账务更正、Redis 清理、真实收费生成、部署、功能开关调整、旧任务重新 Create、降低审批或证据要求。
- 本轮完成条件：检查点可校验、文档没有当前状态冲突、本地回归有退出码与日志、所有外部待决项明确列出。满足后结束本轮，不因发布待决而自动追加代码。
- 本轮结束不等于 F1-F10 全部验收通过，也不等于原长期目标已经完成；真实发布另行授权。

原审查任务 `01a06fad-4b3f-74c2-9add-6be5dbf2dc1c` 在本轮开始时为 idle，最近一轮 interrupted。本轮没有唤醒该任务，也没有修改其内部 goal 状态。后续恢复工作先读本清单，不沿用历史“下一批工作”自动续做。

## 2. 冻结的实现基线

| 范围 | 截至第 24 批的实现 | 本轮处理 |
|---|---|---|
| F1/F5 账户与授权 | 全局 owner、身份版本、调度/分组授权、人工 principal 审批、凭据披露保护 | 保留，首发必须验证跨用户和跨分组拒绝 |
| F2 创建与预算 | 原子 task/hold、预留预算、RPM、JSON/multipart 共享意图、冻结重放 | 保留，不改成所有入口统一硬 reservation |
| F3 已有资源访问 | 生成付费准入与资源访问分离，仍检查身份及 owner | 保留，覆盖零余额/过期 Key/跨用户场景 |
| F4 执行规格 | v2/hash、报价冻结、已知冲突转人工；未知上传只接受独立按次价格 | 保留拒绝规则，可信上传计量另列待决 |
| F6/F9 资金 | 规格与 usage 分离、冻结倍率、入账时间合同、人工复核、不可变 effect archive | 保留原生计费 outbox/dedup/复核；effect archive 的业务消费者仅为已排除的 Grok 更正，随 267 删除 |
| F7/F8/F10 恢复 | 通知义务、先观察/结算后删除、租约续期及代次保护 | 保留，真实故障矩阵仍是发布门禁 |
| Grok 兼容（越界实现） | 耐久作业/回执/后台恢复，旧 Redis 证据归档及人工接管 | 排除，不保留为本次交付要求，不要求补齐或验收 |
| Grok 历史账务更正（越界实现） | preview、proposal、双人批准、原子 apply、独立账本、缓存投影 outbox、API/UI | 排除；记录其代码存在仅为追溯，不代表必须保留 |

Provider registry 当前只接入 OpenAI adapter；Grok 仅保留项目原有网关及最小入口分派，不再有本轮新增的耐久作业或更正链路。

### 2.1 越界实现与剥离边界

| 类别 | 原落点 | 当前处理 |
|---|---|---|
| 新增 Grok 专属服务/仓储 | `backend/internal/service/grok_video_*.go`、`backend/internal/repository/grok_video_*.go` 及其新增测试 | 已删除并解除调用和依赖注入 |
| 新增 Grok 管理面 | `/admin/videos/grok/*` 路由、video admin、`VideoTasksView.vue`、API 和翻译中的 Grok 分支 | 已按分支剥离，OpenAI 任务管理保留 |
| Grok 专属及附属 SQL | 原 255-263、265-269 | 生产未应用，14 个旧文件直接删除；不再安装对应表、函数、触发器。当前 265/266 是另行新增的原生视频迁移 |
| 混合改动 | 254 create intent、multipart、usage billing/outbox、effect archive、Wire、gateway 路由和测试夹具 | 254 仅保留原生状态及保护；删除未消费回执、重复解析和 effect archive，保留原生幂等、资金及恢复保护 |
| 项目原有 Grok 功能 | 原有 `grok_media.go`、账户/OAuth、非本轮新增的网关能力 | 不属于“前一会话新增实现”的自动删除范围；不据此开展兼容改造或存量迁移 |

上述新增服务、复核/更正路由、页面和未发布 SQL 已剥离；原生创建意图继续保留。原有 Grok 网关与最小分派边界不迁移、不扩展。旧归档内 SQL 仅为历史证据，不纳入当前迁移集合。

## 3. 延期与必要性决策

下列延期项只针对 OpenAI 视频交付。Grok 不是延期候选：原 D1（Grok 存量/迁移）与 D6（Grok 历史更正）已按用户决定关闭并排除，不再要求调查或等待批准。

| ID | 项目 | 重新进入实施的条件 | 当前状态 |
|---|---|---|---|
| D2 | 编辑、延长、角色与回调 | 用户另行明确开放并完成对应验收 | 已重新进入实施；客户端回调保持配置级 opt-in |
| D3 | 长期凭据披露、直接上游 file_id、自动 principal 验真 | 必须提供这些能力且有可信 Provider 身份证据合同 | 不追加自动验真；不得把人工审批当外部真实性证明 |
| D4 | 全系统同步/异步硬 reservation | 单独批准跨入口计费改造及兼容/性能预算 | 不纳入视频收口 |
| D5 | 无停机旧新 writer 混跑 | 运维明确不能采用维护窗口，并单独验证所有旧 writer 合同 | 不追加混跑兼容；维护窗口也待运维确认 |

首发范围为 OpenAI API Key、标准余额、创建/编辑/延长/角色/列表/查询/下载/删除及可选回调。具体模型、价格和部署账户仍需确认。本次不交付 Grok 视频兼容、耐久作业、历史接管或账务更正，也不新增相关兼容承诺。

**范围排除已落实到运行时和未发布 SQL。** 当前源码不再安装 Grok correction apply、legacy 扫描/提升和派发复核对象，也没有对应路由和页面；项目原有 Grok 功能仍保留。

## 4. OpenAI 发布环境核对

仅在准备 OpenAI 视频发布时核对实际环境；不盘点 Grok，不将 Grok 存量作为前置条件。未提供经确认的生产环境与只读访问方式，本轮没有连接生产数据库或 Redis。以下项不是已完成检查。

| 盘点项 | 最小输出 | 用途 |
|---|---|---|
| 环境与 schema | 环境标识、盘点时间、已应用迁移和目标表是否存在 | 区分未部署与真实零记录；缺表不能记为 0 |
| OpenAI 视频在途义务（如已有部署） | 按 generation/billing/delete/callback 状态的数量、冻结金额合计、最老时间 | 确定 OpenAI 发布期间必须继续处理的义务 |
| OpenAI 计费一致性 | 任务、hold、capture/release、usage/outbox 的对应关系 | 验证当前任务结算，不扩展到 Grok 历史更正 |
| 身份/凭据需求 | 实际需长期凭据、上游 file_id 的账户数及已验证身份覆盖 | 决定 D3 优先级 |
| 发布拓扑 | gateway/worker/管理节点版本、可用维护窗口、回滚目标 | 决定 D5 与停止旧 writer 的操作范围 |

只读盘点必须使用确认环境的只读角色与有界超时；Redis 使用有界 SCAN，禁止 KEYS、DEL 和全库导出。只保留脱敏聚合，不输出 prompt、token、cookie、完整 Provider 响应或原始 Redis payload。首轮 schema 探测后再按实际表结构选择查询；不猜生产表/字段。

## 5. 发布验收清单

“通过”必须绑定源码快照、命令退出码和具体场景；历史日志、mock、真实数据库集成、真实 Provider、生产结果分别记载。不使用没有分母的完成百分比。

| ID | 门禁 | 当前状态 | 通过条件 |
|---|---|---|---|
| G1 | 源码检查点与无意外修改 | 初始快照已逐文件校验；最终归档与差异证据见检查点 | 归档/清单 hash 校验，暂存区不变，业务源码与冻结前一致或有明确修复记录 |
| G2 | 当前本地自动化 | 本地实施的最新验证见《原生视频首发实施与验收方案》第 6 节；此前结果为历史检查点，保留既有 skip | 每项结果绑定实际测试范围与命令；失败/跳过不记通过，旧结果不冒充重跑 |
| G3 | OpenAI 范围与发行包 | Grok 已排除；D2 已开放；实际模型/价格/环境待确认 | 创建、编辑、延长、角色和配置级回调可用；不要求 Grok 存量盘点 |
| G4 | 真实 Provider 核心闭环 | 手动 smoke 和只读对账工具已完成；真实执行待授权 | 受控账号完成创建、同 key 重放、查询、下载、删除与余额/usage 对账；收费调用需授权 |
| G5 | 真实故障恢复与安全 | 本地子进程退出/恢复和既有安全回归通过；实际拓扑演练未执行 | 提交结果不确定、结算/投影后 ack 前退出、删除与回调恢复、权限变化、跨用户拒绝；多实例接管按实际拓扑验证 |
| G6 | 浏览器验收 | mock API 的桌面/移动端程序检查通过；真实后端及人工视觉验收未执行 | 桌面/移动端任务与管理流程、错误/待处理状态、审批人限制、重复点击和高风险提示 |
| G7 | 迁移/切换/回滚 | PG18.1/15.13 模拟基线升级通过；生产基线、切换和回滚未执行 | 已应用 checksum 不变，目标数据库版本验证、锁/容量评估，旧 writer 隔离及可处理新 outbox 的回滚方案 |
| G8 | 性能与运行观测 | 删除/轮询积压规则及 8 并发隔离冻结校验通过；生产接收与实际负载未执行 | 商定负载下的预算查询/连接池/锁等待；首发 pending/最老任务有告警和处置负责人，不包含已排除的历史归档 |

无真实 Provider 或生产数据时，不继续补写外围功能来替代 G3-G8。该部分等待明确环境、业务决定及授权，不能以本地回归代签。

本次新增迁移集合为 238-254、264、265_video_failed_auto_release.sql 与 266_video_task_provider_url.sql。原 Grok 255-263、265-269 已按生产未应用的前提移除，不能按编号范围把新的原生视频 265/266 判为 Grok。编号不重排，`_notx` 和 checksum 仍遵循原迁移框架；不新增兼容例外。本次不执行生产 DDL，验证使用全新隔离测试数据库。迁移 240 按现有门禁需要 `breaking-migration` 标签及停机发布，具体检查命令见运行手册。

## 6. 本轮验证与检查点

本节记录前一轮收口验证及文档范围纠正；原归档保留，不能把旧归档中的 Grok 范围要求或测试结果当作当前决定和最终证据。代码剥离后的验证与新检查点统一见《通用视频任务平台Grok剥离记录》。

- 基线 HEAD：`933f33e8ca3befc887fa39cc9940ae48b8937fcc`；该 HEAD 不包含全部工作树实现，不能独立代表待交付代码。
- 检查点目录：`/Users/gedebin/Documents/Code/sub2api-checkpoints/video-freeze-20260905T210242-0700`。
- 冻结前源码：`source-before-closeout.tar.gz`，SHA-256 `804b1caac669ce5915993e31b672c960b3344638af3d08bada380de7b057c0a4`。
- 清单：`source-manifest.json`，覆盖 4,558 个工作树文件，含非忽略的未跟踪源码；原有三个 `deploy/*.mp4` 未归档且未改动。依赖缓存、运行环境、数据库、Git 历史不包含在源码包中。
- `staged.patch`、`unstaged.patch` 和 `git-status.before.txt` 分别保留原暂存/未暂存差异与状态；未提交 Git、未创建分支、未使用 stash/reset，也未改变原暂存区。
- 原始源码包包含冻结前文档；最终 `source-final.tar.gz`、`final-manifest.json`、`validation-results.json` 和 `CHECKSUMS.sha256` 另行生成，包含下方 B1 最小修复、最终文档和证据索引，不覆盖原检查点。校验说明见检查点目录的 `README.md`。

| 检查 | 本轮命令 | 结果 / 证据文件 |
|---|---|---|
| 后端默认 | `go test ./... -count=1 -timeout=5m` | 冻结前基线通过，退出码 0；`backend-default.log`。B1 后另跑完整 config 默认套件，不冒充全量默认重跑 |
| 后端 unit | `go test -tags=unit ./... -count=1 -timeout=5m` | 首轮失败，退出码 1，见 B1；`backend-unit.log`。修复后完整重跑通过，退出码 0：`backend-unit-after-fix.log` |
| 仓储集成 | `CI=true go test -json -tags=integration ./internal/repository -count=1 -timeout=5m` | 冻结前基线退出码 0；826 个顶层用例通过，含子测试共 1884 pass / 1 skip；`backend-integration.jsonl` |
| 定向 race | `go test -race ./internal/service ./internal/server/middleware ./internal/server/routes -run 'Video\|AccountOwnership\|AccountProviderIdentity' -count=1 -timeout=5m` | 冻结前基线三个包通过，退出码 0；`backend-race.log`，不代表全项目 race |
| Go 静态检查 | `go vet ./...`；B1 后 `go vet ./internal/config` | 基线全量及修复后 config 均通过；`backend-vet.log` / `config-vet-after-fix.log`，退出码 0 |
| 前端类型 | `npm run typecheck` | 通过；`frontend-typecheck.log`，退出码 0 |
| 前端测试 | `npm run test:run` | 328 文件 / 2415 Vitest + 4 entry privacy 通过；`frontend-tests.log`，退出码 0 |
| 前端构建 | `npm run build` | 通过，entry privacy pass；`frontend-build.log`，退出码 0，保留 chunk size 等非阻塞警告 |
| 差异与归档 | `git diff --check HEAD`、逐文件 SHA-256/归档校验 | 差异检查通过、原始 4,558 文件已解压校验；最终校验及变更列表见 `validation-results.json` / `final-manifest.json` |

上述是一次收口回归，不重新执行所有历史故障实验。未列出的 lint、监控规则、生成代码重建、真实浏览器/Provider 和生产演练不能视为本轮通过；冻结后的任何业务代码变化都需要重验受影响门禁。

### 6.1 B1：仅修复验收发现的环境变量缺口

- 复现：完整 tagged unit 的 `TestConfigKeysAreEnvReachable` 失败，指出 `totp.legacy_encryption_keys` 未注册默认值；纯环境变量部署会忽略 `TOTP_LEGACY_ENCRYPTION_KEYS`。旧钥已配置却未加载，可能导致轮换后的历史密文无法解密。
- 新回归：`backend/internal/config/totp_env_test.go` 覆盖空配置、单个旧钥、逗号列表、空白/重复项/当前钥过滤，以及当前加密钥保持不变。只加测试时单旧钥和多旧钥断言失败，退出码 1，证据 `totp-env-before-fix.log`。
- 最小修复：`backend/internal/config/config.go` 只增加 `viper.SetDefault("totp.legacy_encryption_keys", []string{})`，不修改加解密算法、不增加迁移、不改运行环境密钥或审批策略。这是收口缺陷修复，不是新增第 25 批能力。
- 复验：完整 config 默认套件、完整 tagged unit、config vet 均退出码 0；日志分别为 `config-default-after-fix.log`、`backend-unit-after-fix.log`、`config-vet-after-fix.log`。新测试格式检查 `gofmt -d` 无输出。
- 其余后端及前端业务实现与初始快照一致；数据库/资金状态机未改变，先前通过的仓储集成及定向 race 保留为明确标注的基线证据，不写成修复后重跑。

### 6.2 已有覆盖缺口

`TestConcurrencyCacheSuite/TestGetAccountsLoadBatch` 在仓库源码中显式 `Skip`（`concurrency_cache_integration_test.go:525`），理由是 CI 中 CurrentConcurrency 与预期不符。本轮没有忽略失败或新增 Skip，不以 826 个顶层用例通过推导“0 跳过”。这项缓存负载覆盖保留给 G8 独立验证，不在文档收口中展开无关重构。

## 7. 后续工作入口

后续按《原生视频首发实施与验收方案》确认实际模型、价格、测试账户及发布环境，执行基础能力门禁。D2 已明确关闭，不再要求补齐；D3 未另行批准前不扩展凭据披露。不再询问是否存在 Grok 存量，不触碰项目原有功能或盲目回退共享底座。每个后续工作包必须说明范围、验收用例和停止条件；不再重建已删除的 Grok 能力。

本轮 G2 已执行结束、B1 已修复复验、既有 skip 已登记；封存最终检查点后停止，保持“本地冻结、生产未验收”的准确状态，不把 G3-G8 的外部待决事项自动转换成下一批代码任务。
