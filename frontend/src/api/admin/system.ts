/**
 * System update/version API wrappers were intentionally removed from the
 * frontend console. The backend endpoints remain available for non-UI callers.
 */
export const systemAPI = {}

export default systemAPI
